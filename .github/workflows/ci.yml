name: CI
  on: [push, pull_request]
  jobs:
    build:
      runs_on: ubuntu-latest
      steps:
        - uses: actions/checkout@v4
        - uses: actions/setup-go@v5
          with:
            go-version: '1.21'
        - run: go build ./...
        - run: go test ./...
        - run: go vet ./...